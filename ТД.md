# Vervel — Техническое задание

> Фитнес-платформа для тренеров и атлетов с AI-функциями, аналитикой нагрузок и системой коммуникации.

---

## 0. Описание продукта

**Vervel** — связка инструментов для учёта тренировок, наглядной обратной связи по нагрузке (2D-аватар, зоны тела), планирования и общения тренер↔атлет, с опциональным AI и монетизацией AI через внутренний баланс.

### Роли

| Роль | Назначение |
|------|------------|
| `athlete` | Свои тренировки, аналитика, команда, чаты с тренером |
| `trainer` | Атлеты, группы, календарь, шаблоны, данные по атлетам |
| `both` | Обе роли; в UI переключается активный кабинет (тренер / атлет) |

### Атлет

- Календарь активности, детали тренировок, черновик тренировки.
- Создание тренировки вручную; **AI**: распознавание по фото, генерация по тексту, парсинг заметок и применение к тренировке.
- Экран **Avatar** — нагрузка и восстановление по зонам (модель с затуханием).
- **Аналитика** — период, зоны, графики.
- **Сила и прогресс** — единый экран силового журнала и дашборда упражнений (`/progression`); детали в **разделе 7**.
- **Периодизация** — ATL / CTL / TSB (PMC, Banister); экран аватара — **`/home`** (не корень `/`, там лендинг).
- **Серии и достижения** — streak, история, разблокировка и просмотр ачивок.
- **Команда** — группы и тренеры, инвайты (ссылка / QR), публичный профиль тренера.
- **Лидерборд** по группе.
- **Диалоги** (`/dialogs`) — список чатов, сообщения, **SSE-поток** обновлений, отметка прочитанного; вложения **GIF через Klipy** (поиск/категории при включённом бэкенд-провайдере).
- **Входящий видеозвонок** — опрос, приём/отклонение, LiveKit.
- **Профиль** — данные, фото, пароль, смена роли, замеры тела, тема, кошелёк AI и пополнение (ЮKassa), реферальная статистика.

### Тренер

- Обзор **«Сегодня»**, календарь и плановые тренировки (CRUD).
- **Команда** — атлеты и группы; карточка атлета (статы, аватар нагрузки, периодизация, тренировки и др.), **видеозвонок** 1:1 или группе.
- **Шаблоны** тренировок и **библиотека упражнений**.
- **Чаты** — личные и групповые.
- История/управление **видеозвонками** (API).

### Общее

- **Авторизация:** email/пароль (rate limit), OAuth VK и Яндекс (в т.ч. **VK ID SDK** / **Яндекс SDK** в вебе), **VK Mini Apps** (`POST /oauth/vk/mini-app-login` с проверкой подписи launch params); выбор роли на `/select-role` при `role=null`; доступ по httpOnly cookie `auth_token` (на сервере подстановка в Bearer для guard).
- **AI:** баланс, транзакции, статус; тарификация чата по токенам.
- **Платежи:** пополнение кошелька, вебхук ЮKassa.
- **Web Push:** VAPID, подписка/отписка.
- **Обратная связь** с привязкой к пользователю.
- **PWA:** service worker (офлайн-страница, push).

### Публичная часть

- Лендинг, вход/регистрация, OAuth callback, приглашение по ссылке, документы (privacy / offer / seller).

---

## 1. Стек технологий

### Backend (`apps/api`)
| Пакет | Версия | Роль |
|-------|--------|------|
| AdonisJS | ^6.18 | Web-фреймворк (MVC) |
| Lucid ORM | ^21.6 | ORM для PostgreSQL |
| @adonisjs/auth | ^9.4 | JWT-аутентификация |
| @adonisjs/limiter | ^2.4 | Rate-limiting (БД) |
| VineJS | ^3.0 | Валидация входных данных |
| Luxon | ^3.7 | Работа с датами |
| fuzzball | ^2.2 | Fuzzy-матчинг упражнений |
| livekit-server-sdk | ^2.15 | Токены и серверная логика звонков |
| web-push | ^3.6 | Web Push |
| sharp, heic-convert | — | Обработка изображений тренировок |
| PostgreSQL | 16 | БД |

### Frontend (`apps/web`)
| Пакет | Версия | Роль |
|-------|--------|------|
| React | ^19.2 | UI |
| React Router | ^7.12 | SPA-роутинг |
| Vite + Tailwind v4 | latest | Сборка и стили |
| axios | ^1.13 | HTTP-клиент |
| recharts | ^3.7 | Графики |
| framer-motion | ^12.33 | Анимации |
| @headlessui/react | ^2.2 | Компоненты (модалки, попапы) |
| @heroicons/react | ^2.2 | Иконки |
| react-hot-toast | ^2.6 | Уведомления |
| jsqr + qr-code-styling | — | QR-код сканирование/генерация |
| @livekit/components-react, livekit-client | ^2.x | Видеозвонки (UI + клиент) |
| @vkid/sdk, @vkontakte/vk-bridge | — | VK OAuth и мини-приложения |

---

## 2. Архитектура

```
vervel/
├── apps/
│   ├── api/          — AdonisJS backend (Node.js, TypeScript)
│   │   ├── app/controllers/   — 19 контроллеров
│   │   ├── app/models/        — 25 моделей Lucid
│   │   ├── app/services/      — модули доменной логики (~20+ файлов: сервисы, хелперы цепочек AI и т.д.)
│   │   ├── app/utils/         — утилиты (в т.ч. проверка VK Mini App launch params)
│   │   ├── database/migrations/
│   │   └── start/routes.ts    — все HTTP-роуты
│   └── web/          — React SPA (TypeScript, Vite, PWA)
│       ├── src/screens/
│       ├── src/components/
│       ├── src/api/
│       └── src/constants/routes.tsx
├── packages/         — зарезервировано под общие пакеты (workspaces; может быть пустым)
├── docker-compose.yml        — postgres + api + web (+ livekit в compose при необходимости)
└── docker-compose.dev.yml    — опциональный dev-web с hot reload
```

Локальный поток без Docker для API+web: `npm run dev` в корне (поднимает Postgres через `docker compose -f docker-compose.yml`, затем concurrently api+web).

### Аутентификация
- Email/пароль с rate-limiting (10 попыток/15 мин)
- JWT access tokens (хранятся в БД через Lucid Access Tokens)
- OAuth: VK, Яндекс
- Honeypot-защита от ботов на регистрации
- Middleware `auth()` + `trainer()` (роль)

---

## 3. Роли пользователей

| Роль | Описание |
|------|----------|
| `athlete` | Записывает тренировки, видит аналитику, общается с тренером |
| `trainer` | Управляет атлетами и группами, назначает тренировки |
| `both` | Обе роли одновременно |

---

## 4. Модели БД

### User
```
id, fullName, email, password (nullable — для OAuth),
role: athlete|trainer|both|null (null — до выбора роли после OAuth / мини-приложения),
gender, bio, specializations (JSON), education, photoUrl,
balance (₽, AI-кошелёк), aiNotesFree (bool, бета/админ),
themeHue (0–359), xp, referredById,
donatePhone, donateCard, donateYookassaLink (реквизиты «доната» тренеру),
createdAt, updatedAt
```

### Workout
```
id, userId, date, workoutType: crossfit|bodybuilding|cardio,
exercises (JSON), zonesLoad (JSON), totalIntensity, totalVolume,
notes, scheduledWorkoutId (nullable), deletedAt (soft delete)
```

### Exercise (каталог, статический)
```
id (string), title (EN), category, keywords[], zones[], intensity (0-1)
```

### TrainerAthlete (связь тренер↔атлет)
```
id, trainerId, athleteId (nullable — для pending invite),
status: active|pending|inactive, inviteToken (UUID)
```

### TrainerGroup
```
id, trainerId, name — athletes через pivot group_athletes (soft delete)
```

### Chat + Message + ChatRead
```
Chat: id, type: group|personal, trainerId, groupId?, athleteId?
Message: id, chatId, senderId, content, createdAt
ChatRead: id, chatId, userId, lastReadAt
```

### ScheduledWorkout
```
id, trainerId, athleteId?, groupId?, scheduledDate,
workoutData (JSON), status: scheduled|completed|cancelled, assignedTo
```

### WorkoutTemplate
```
id, trainerId, name, workoutType, exercises (JSON), notes
```

### UserStreak
```
id, userId, currentStreak, longestStreak, lastWorkoutDate, streakStartedAt
```

### StreakHistory (audit log)
```
id, userId, date, eventType: workout_completed|streak_continued|streak_broken|new_record,
streakValue, metadata (JSON)
```

### Achievement + UserAchievement
```
Achievement: id, key, category: streak|workout|usage|social|progress,
  title, description, icon, color,
  requirementType, requirementValue (nullable), isActive
UserAchievement: userId, achievementId, unlockedAt, isSeen
```

Типы `requirementType` (основные): `streak_days`, `streak_intensive_weeks`, `total_workouts`, `ai_chat_messages`, `groups_joined`, `trainer_messages`, `trainers_connected`, `personal_records`, `progression_coeff` (порог коэффициента прогрессии из `ProgressionService`).

### WorkoutDraft
Черновик формы тренировки на пользователя: `userId`, `payload` (JSON), timestamps. Роуты `GET|PUT|DELETE /workouts/draft`.

### VideoCall
История и состояние звонков LiveKit: `roomName`, `trainerId`, `athleteId?`, `groupId?`, `status`, `startedAt`, `endedAt`.

### PushSubscription
Подписки Web Push (endpoint, ключи), привязка к `userId`.

### Topic
Темы/ветки в групповом чате: привязка к `groupId`, `chatId`, имя, emoji, порядок.

### ExerciseAnatomyCache
Кэш анатомических данных по упражнению (сервис `ExerciseAnatomyService`).

### UserPinnedExercise (силовой журнал — закрепления)
```
id, userId, exerciseId (string, до 512), createdAt
UNIQUE(userId, exerciseId)
```

### UserExerciseStandard + UserExerciseStandardAlias (эталоны силового журнала)
```
UserExerciseStandard: id, userId, displayLabel, catalogExerciseId (nullable, id из каталога),
  createdAt, updatedAt
  — частичный UNIQUE(userId, catalogExerciseId) при NOT NULL catalogExerciseId

UserExerciseStandardAlias: userId, sourceExerciseId (string, сырое id из тренировок),
  standardId → UserExerciseStandard, createdAt
  UNIQUE(userId, sourceExerciseId)
```
Алиас задаёт отображение строки журнала в группе эталона (`std:{id}`); цепочек алиасов нет.

### UserExerciseStandardLinkBatchSnapshot (снимок пакетного применения алиасов)
Используется для **безопасного применения** предложенных ИИ связей (или ручного батча) с возможностью **отката** одним действием.
```
user_exercise_standard_link_batch_snapshots:
  id, userId,
  touchesJson (JSONB): [{ source: string, beforeStandardId: number|null }],
  createdAt
INDEX(userId, createdAt DESC)
```

### BalanceTransaction
```
id, userId, amount, balanceAfter, type: charge|topup, description, createdAt
```

### Payment (ЮКасса)
```
id, userId, yookassaPaymentId (UNIQUE), amount, status: pending|succeeded|canceled,
idempotencyKey
```

### OAuthProvider
```
id, userId, provider: vk|yandex, providerId
```

---

## 5. API — основные роуты

Полный перечень — `apps/api/start/routes.ts`. Ниже сгруппировано по назначению.

### Публичные
- `POST /login`, `POST /register`, `POST /logout` (logout с auth)
- `GET /exercises`, `GET /exercises/:id` — каталог
- `GET /invite/info/:token`
- `POST /payments/webhook` — ЮKassa (IP-whitelist)
- OAuth: `GET /oauth/:provider/redirect|callback`, `POST /oauth/set-role`, `POST /oauth/vk/sdk-login`, `POST /oauth/vk/mini-app-login`, `POST /oauth/yandex/sdk-login`
- Push: `GET /push/vapid-key`

### С auth (префикс корня, группа `middleware.auth()`)
**Тренировки и тело:** `resource workouts` (apiOnly) + `GET/POST/PUT/DELETE workouts/draft`, `GET workouts/stats`, `GET workouts/by-zone`, `GET workouts/by-scheduled/:scheduledWorkoutId`, `GET avatar/stats`.

**Профиль:** `GET|PUT /profile`, `POST /profile/photo`, `PUT /profile/password`, `POST /profile/become-athlete|become-trainer`, замеры `POST|GET /profile/measurements`, `DELETE /profile/measurements/:id`, `GET /athlete/trainers/:trainerId/profile` (публичная карточка тренера).

**Серии:** `GET /streak`, `GET /streak/history`, `PATCH /streak/mode`.

**Прогрессия:** `GET /progression` (сводка пользователя), `GET|PUT …/strength-log`, `…/pins`, CRUD эталонов и алиасов, AI-подсказки связей и батч-откат (как в разделе 7), `GET /athlete/groups/:id/leaderboard`.

**Ачивки:** `GET /achievements`, `POST /achievements/check`, `POST /achievements/seen`.

**Инвайты и реферал:** `POST /invite/accept`, `GET /profile/qr-data`, `GET /referral/stats`.

**Звонки (атлет/тренер):** `POST /calls/:roomName/join|decline`, `GET /athlete/calls/active`.

**Атлет:** `GET /athlete/my-groups`, `my-trainers`, `unread-counts`, `upcoming-workouts`, `periodization`, `GET /athlete/chats/group/:groupId`, `…/trainer/:trainerId`.

**Чаты (общий доступ участника):** `GET /chats`, `GET /chats/:chatId/stream` (SSE), `GET|POST /chats/:chatId/messages`, `POST /chats/:chatId/read`; **Klipy:** `GET /chats/klipy/status`, `…/categories`, `…/search`.

**Прочее:** `POST /feedback`, `POST|DELETE /push/subscribe|unsubscribe`, `POST /payments/topup`.

### AI (`/ai/*`, auth)
- `GET /ai/status`, `balance`, `transactions`
- `POST /ai/recognize-workout`, `generate-workout`, `parse-workout-notes`, `parse-notes-text`, `apply-parsed-workout`, `chat`

### Тренер (`/trainer/*`, auth + `middleware.trainer()`)
- `GET today`, `profile-stats`; `GET unread-counts`
- Атлеты: список, by-email, invite, by-qr, `DELETE …/:athleteId`, `PATCH …/nickname`, `GET …/stats|avatar|periodization|workouts`
- Группы: CRUD, атлеты в группе, `GET …/leaderboard`
- Расписание: `scheduled-workouts` (+ `today`)
- Шаблоны: `workout-templates`
- Чаты: `GET chats/group/:groupId`, `…/athlete/:athleteId`, сообщения + last
- Звонки: `POST /trainer/calls`, `POST …/:callId/end`, `GET /trainer/calls` (история)

---

## 6. Сервисы и модули логики (`apps/api/app/services/`)

Ключевые: **WorkoutCalculator**, **PeriodizationService**, **ProgressionService** (+ `strength_log_support`, `exercise_match_helpers`), **StreakService** (+ `streak_logic`), **AchievementService**, **XpService** (+ `xp_logic`), **AiBalanceService**, **YandexAiService** (+ цепочки `ai_parse_chain`, `ai_workout_ocr_parse`, **WorkoutConverter**), **ExerciseCatalog**, **LiveKitService**, **PushNotificationService**, **DialogService** (+ `chat_stream_logic`), **KlipyService**, **ExerciseAnatomyService**, **AiZonesService**, **call_logic**.

### WorkoutCalculator
Расчёт нагрузки по зонам тела при создании тренировки.

- **bodybuilding**: `weight × reps`, нормализуется к `[0..1]`
- **crossfit**: `rounds / MAX_ROUNDS_FOR_FULL_LOAD`
- **cardio**: `duration / MAX_TIME (3600с)`
- **calculateRecoveryState** — декаментное затухание нагрузки (λ=0.3) для модели восстановления
- **calculatePeriodStats** — агрегация за период: объём, интенсивность, breakdown по типам

### AiBalanceService
Кошелёк для AI-функций.

- `charge(userId, amount, description)` — списание с транзакцией БД
- `calculateChatCost(inputTokens, outputTokens)` — стоимость = `tokens/1000 × rate × markup`, мин. `CHAT_MIN_CHARGE`
- `InsufficientBalanceError` — бросается при нехватке средств

### YandexAiService
- `recognizeFromImage` — OCR (Yandex Vision) → LLM-парсинг → структурированная тренировка
- `generateFromText` — описание → тренировка через YandexGPT
- `chat` — история сообщений → ответ + `{ inputTokens, outputTokens }`

### StreakService
- Логика streak: тот же день → без изменений; +1 день → `currentStreak++`; пропуск → `currentStreak=1`
- Логирует каждое событие в `streak_history`
- После обновления вызывает `AchievementService.checkAndUnlockAchievements`

### AchievementService
Разблокировка по `requirementType` (см. раздел 4); при выдаче начисляет XP через **XpService** (размер награды зависит от `category`, для `progress` — крупная награда).

### ExerciseCatalog
Статический каталог. Fuzzy-матчинг (fuzzball `token_set_ratio ≥ 72`) для AI → каталог.

### ProgressionService
Силовой журнал, закрепления, **эталоны** (`user_exercise_standards` + алиасы), условный 1RM (формула Эпли), группировка по эталону или сырому `exerciseId`, обогащение названий (БД `exercises`, `ExerciseCatalog`, `capitalizeFirstForDisplay`). В payload каждой записи журнала — **`dashboardMetric`** (окна 30+30 дней, как раньше у отдельного дашборда). Детальное ТЗ — **раздел 7**.

---

## 7. Сила и прогресс (силовой журнал, эталоны, сводка по карточке)

Функциональность для атлета: наглядно видеть работу с весом по упражнениям, объединять разные id одного движения через **эталоны**, и видеть **краткую сводку 30+30 дней** прямо в карточке.

### 7.1. Продукт и навигация

- **Экран** `/progression` — **один** поток контента: встроенный **`StrengthLogScreen`** (`embedded`), без отдельной вкладки «Дашборд».
- Вход с **Аналитики**: блок быстрых ссылок «Сила и прогресс» → `/progression`.
- **Редиректы** (старые закладки): `/strength-log` и `/exercise-dashboard` → `/progression` (без `?tab=`).
- Вёрстка: `ProgressionHubScreen`, заголовок, **`SectionDivider`**, журнал.

### 7.2. Силовой журнал

**Источник данных**

- Тренировки пользователя за **около года** (`Workout`, не удалённые), упражнения с **хотя бы одним подходом с весом > 0** и непустыми сетами.
- Учитываются и **силовые**, и **WOD** (тип тренировки не отсекает запись).
- На упражнение показываются до **6 последних сессий** (по разным тренировкам).

**Закрепления (pins)**

- Список id хранится в таблице **`user_pinned_exercises`** (см. раздел 4).
- **Пустой список пинов** — в журнале отображаются **все** подходящие упражнения; API отдаёт **`suggestedPins`** — топ по числу сессий с весом (для кнопки «закрепить топ-N»).
- **Непустой список** — в журнале только закреплённые id; порядок карточек = порядок пинов.
- Добавить в пины можно из **истории за год** (все упражнения с весом), включая **`custom:…`** после ИИ — отдельный bottom sheet, не обзор каталога ~800 упражнений без истории.
- Важно: в режиме пинов карточка **не строится**, если по этому id **нет ни одной сессии с весом** в выборке (нет «пустых» карточек только из каталога).

**Платная подсказка ИИ: «связи с эталонами»**

- Цель: ускорить группировку «одного и того же движения» под уже созданные **эталоны** (стандарты) без слепого применения.
- Кнопка появляется, если:
  - есть хотя бы один эталон (`standards.length > 0`);
  - есть хотя бы одно **несвязанное** упражнение из истории с весом (нет алиаса и нет совпадения с `catalogExerciseId` эталона);
  - AI-функции включены (`FEATURE_AI_WORKOUT=true` → `GET /ai/status` возвращает `enabled=true`);
  - баланс достаточен для списания (UI делает disabled при балансе ниже стоимости).
- Ограничение объёма: в один запрос к ИИ уходит максимум **N** упражнений из истории (параметр сервера `AI_STANDARD_LINK_SUGGEST_MAX_CANDIDATES`, отдаётся в payload `aiStandardLinkSuggestMaxCandidates`).
- Оплата:
  - это **платная услуга**, списание `AI_COST_SUGGEST_STANDARD_LINKS` ₽ за запрос (по умолчанию 8 ₽);
  - списание происходит **в момент отправки** запроса на подсказки;
  - повтор запроса = **новое** списание.
- UX:
  - результаты показываются в bottom sheet со списком предложений и чекбоксами;
  - пользователь выбирает, что применить;
  - после применения показывается уведомление с кнопкой **«Откатить»** (одним действием).

**Карточка упражнения (UI)**

- Таблица: подходы × даты сессий, строка **1RM** (с бэкенда, Эпли по подходам сессии).
- Переключение **таблица / график / сводка 30 дней** (три режима); график — **Recharts** `LineChart`, ось Y — **best1RM** сессии или при отсутствии — **максимальный вес** в подходах.
- Режим **сводки** — поля из **`dashboardMetric`** в записи журнала (лучший 1RM за 30 дн., изменение % к предыдущим 30 дн., число сессий, дата последней тренировки).
- Кнопка **эталона** (sheet): создать эталон и привязать текущий сырой `exerciseId`, опционально **каталог** через `ExercisePicker`; для уже существующего эталона — переименование (PATCH) и добавление **синонимов** из списка упражнений с весом за год (ещё не занятых алиасом).
- Кастомный **тултип** точки графика (без переноса «кг»).
- Под названием — **прирост в %** от **самой ранней** к **самой поздней** сессии среди показанных (минимум две сессии с валидной метрикой); расчёт в `strengthLogChart.ts` (`strengthLogProgressPercent`), согласован с метрикой графика.

**Названия упражнений**

- Обогащение на бэкенде: `exercises.title` из БД, иначе заголовок из **`ExerciseCatalog`**, нормализация подчёркиваний, **`capitalizeFirstForDisplay`** для отображаемой первой буквы (в т.ч. если в JSON/тренировке строка со строчной).

### 7.3. Эталоны и метрика «как дашборд»

- Отдельной сущности «список упражнений дашборда» (**`user_dashboard_exercises`**) нет: сводка 30+30 считается **для каждой карточки журнала** (в т.ч. для группы эталона) и отдаётся в **`dashboardMetric`**.
- Объединение разных строк (`custom:…`, каталог, ИИ) — через **эталон** и алиасы `sourceExerciseId → standardId` (без цепочек).

### 7.4. Backend (реализация)

- **`ProgressionService`** (`apps/api/app/services/ProgressionService.ts`) — сбор журнала, пины, эталоны/алиасы, группировка, расчёт сессий, 1RM и **`dashboardMetric`** по записи.
- **`strength_log_support.ts`** — чистые функции для нормализации списка пинов и топа по частоте (юнит-тесты).
- **`progression_controller`** + роуты в `start/routes.ts` (см. раздел 5).
- ИИ-подсказки для эталонов:
  - `POST /progression/ai-suggest-standard-links` — списывает баланс и возвращает предложения (строгий JSON).
  - `POST /progression/apply-standard-alias-batch` — применяет батч в транзакции и сохраняет снимок `user_exercise_standard_link_batch_snapshots`.
  - `POST /progression/revert-standard-alias-batch/:id` — откат по `revertId` (снимок удаляется, повторный откат невозможен).
- Вспомогательные правила матчинга имён — **`exercise_match_helpers`**, каталог — **`ExerciseCatalog`**.

### 7.5. Frontend (реализация)

- `apps/web/src/screens/ProgressionHubScreen/ProgressionHubScreen.tsx`
- `StrengthLogScreen/`, `strengthLogChart.ts` (+ тесты Vitest для графика/процента).
- API-методы в `apps/web/src/api/athlete.ts` (`getStrengthLog`, `putStrengthLogPins`, CRUD эталонов и алиасов).

---

## 8. Модель периодизации (PMC — Banister)

```
TL  = (intensity×0.7 + min(volume/5000, 1)×0.3) × 100   — нагрузка дня
ATL = ATL_prev × (6/7) + TL × (1/7)    — 7-дн. EWMA  (усталость)
CTL = CTL_prev × (41/42) + TL × (1/42) — 42-дн. EWMA (форма)
TSB = CTL - ATL                          — свежесть
```

| TSB | Фаза |
|-----|------|
| > +15 | Deload / пик готовности |
| 0…+15 | Оптимальная форма |
| −20…0 | Накопление / рост формы |
| < −20 | Перетренированность |

---

## 9. Экраны приложения

Навигация в нижнем баре строится из `athleteRoutes` / `trainerRoutes` в `routes.tsx`; отдельные маршруты без пункта в баре заданы в `App.tsx`.

### Атлет
| Путь | Экран |
|------|-------|
| `/dialogs` | DialogsScreen — чаты, SSE, Klipy |
| `/analytics` | AnalyticsScreen — статистика + радар зон |
| `/calendar` | ActivityScreen — календарь тренировок |
| `/home` | AvatarScreen — восстановление мышц (в баре подпись «Avatar») |
| `/my-team` | AthleteMyTeamScreen — команда |
| `/streak` | StreakScreen — серии и достижения (часто без иконки в баре — вход из профиля) |
| `/profile` | ProfileScreen — профиль, кошелёк, настройки |
| `/workouts/new` | WorkoutForm — ручной ввод / AI |
| `/progression` | ProgressionHubScreen — силовой журнал; редиректы `/strength-log`, `/exercise-dashboard` → сюда |
| `/groups/:groupId/leaderboard` | LeaderboardScreen |

### Тренер
| Путь | Экран |
|------|-------|
| `/dialogs` | DialogsScreen |
| `/trainer` | TrainerTodayScreen |
| `/trainer/team` | TrainerTeamScreen — сводная «команда» (группы + атлеты в одном потоке) |
| `/trainer/groups`, `/trainer/athletes` | списки (доступ из team и др.) |
| `/trainer/calendar` | TrainerCalendarScreen |
| `/trainer/templates` | TrainerTemplatesScreen |
| `/trainer/library` | TrainerExerciseLibraryScreen |
| `/trainer/personal` | TrainerPersonalScreen |
| `/profile` | ProfileScreen |
| `/trainer/athletes/:athleteId` | TrainerAthleteDetailScreen |
| `/trainer/groups/:groupId` | TrainerGroupDetailScreen |
| `/trainer/profile/:trainerId` | TrainerPublicProfileScreen |

### Публичные
| Путь | Экран |
|------|-------|
| `/` | LandingScreen |
| `/login`, `/register` | LoginScreen, RegisterScreen |
| `/auth/callback` | OAuthCallbackScreen |
| `/select-role` | SelectRoleScreen |
| `/invite/:token` | InviteScreen |
| `/docs/privacy`, `/docs/offer`, `/docs/seller` | DocsScreen |

---

## 10. Оплата (ЮКасса)

1. Пользователь выбирает сумму (100/250/500/1000₽)
2. `POST /payments/topup` → создаёт платёж ЮКасса → возвращает `confirmationUrl`
3. Редирект на страницу оплаты ЮКасса
4. После оплаты → `POST /payments/webhook` (IP-whitelist ЮКасса)
5. Backend: `balance += amount`, запись в `balance_transactions type='topup'`
6. Frontend: `/profile?topup=success` → toast + обновление баланса

---

## 11. AI-флоу создания тренировки

```
Фото / текст / заметки → YandexAiService (+ OCR Vision при фото) → структура тренировки
↓
ExerciseCatalog.fuzzyMatch(name) → exerciseId из каталога (где возможно)
↓
Редактирование в UI (в т.ч. после parse-notes / apply-parsed-workout)
↓
POST /workouts → WorkoutCalculator → зоны, streak, achievements
```

Дополнительно: эндпоинты **parse-workout-notes** / **parse-notes-text** / **apply-parsed-workout** для пошагового парсинга без немедленного сохранения.

---

## 12. Деплой

- **Docker Compose** (корень репозитория): `postgres`, `api`, `web`, **`livekit`** (образ `livekit/livekit-server`, конфиг по умолчанию `livekit.yaml`, переменная `LIVEKIT_CONFIG`)
- API: порт **3333**; web-образ отдаёт сборку на **80** внутри контейнера, снаружи часто **5173**
- БД: PostgreSQL 16, volume `pgdata`; отдельные named volumes для `node_modules` api/web
- CI: push в `master` → GitHub Actions (см. README)
- Прод-сервер и домен описаны в корневом **README.md**

---

## 13. Тесты

```
apps/api/tests/
├── unit/
│   ├── workout_calculator.spec.ts   — расчёт зон, нормализация, period stats
│   ├── ai_balance.spec.ts           — calculateChatCost, InsufficientBalanceError
│   ├── achievement_logic.spec.ts   — checkAchievementCondition (все типы)
│   ├── exercise_match_helpers.spec.ts — токены, capitalize, матч к каталогу
│   ├── strength_log_support.spec.ts — нормализация пинов, топ по частоте сессий
│   └── progression_logic.spec.ts    — epley1RM, maxEpley
└── functional/
    └── routes.spec.ts               — ~100 тестов: 401/403/happy-path по основным роутам
```

`apps/web`: Vitest — например `StrengthLogScreen/strengthLogChart.test.ts` (точки графика, % прироста).

Запуск API: `cd apps/api && node ace test`

---

## 14. Зрелость продукта и рыночный контекст (оценка, апрель 2026)

### Техническая зрелость
Проект выглядит **жизнеспособным как продуктовая кодовая база**: полноценный B2C+B2B2C контур (атлет + тренер), монетизация AI через внутренний баланс и **ЮKassa**, интеграции под **РФ** (VK, Яндекс, Yandex Cloud AI), PWA, push, видео (**LiveKit**), обширное **API** и **функциональные тесты** роутов. Это уровень зрелости выше «MVP-наброска»: есть доменная глубина (периодизация, прогрессия, эталоны упражнений, streak/XP/ачивки).

Риски сопровождения: высокая связность фич (чаты + звонки + AI + платежи) требует дисциплины в безопасности, наблюдаемости и compliance; внешние API (Vision/GPT-аналоги, Klipy, LiveKit) — точки отказа и статьи расходов.

### Коммерческая жизнеспособность на российском рынке
**Шансы есть**, ниша понятная: онлайн-клиентинг для фитнеса/кроссфита и «умный дневник» с **локальными** способами входа и оплаты — сильная сторона для РФ по сравнению с западными сервисами без нормальной локализации платежей и OAuth.

Конкуренция **жёсткая**: Strava, Hevy, тренерские CRM, чаты в Telegram, Google Таблицы — бесплатные привычные заменители. Выигрыш Vervel — в **связке** тренер↔атлет (расписание, чат, звонок, нагрузка на одной платформе) и в **AI на Yandex** без привязки к недоступным в РФ моделям/платежам. Узкое горло — **привлечение тренеров** (multi-sided marketplace): без сети тренеров сложнее удерживать атлетов.

**Итог:** как технологический и продуктовый задел платформа выглядит реалистично; коммерческий успех на РФ в большей степени от **go-to-market** (тренеры, залы, партнёрства), ценообразования AI и доверия к бренду, чем от недостатка функций в коде.
