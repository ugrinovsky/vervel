port: 7880
rtc:
  tcp_port: 7881
  use_external_ip: true
keys:
  # Replace with your production key/secret (same values as in prod .env)
  # Generate secret: openssl rand -hex 32
  vervel-prod: <YOUR_PROD_SECRET>
logging:
  level: info
